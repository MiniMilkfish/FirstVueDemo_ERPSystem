import state from '../state/stateSupplyChannel';
import actions from '../action/actionSupplyChannel';
import mutations from '../motation/motationSupplyChannel';

export default {
    // namespaced: true,
    state,
    actions,
    mutations,
    gettters: {}
}