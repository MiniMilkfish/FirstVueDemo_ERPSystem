import state from '../state/stateProductList';
import actions from '../action/actionProductList';
import mutations from '../motation/motationProductList';

export default {
    // namespaced: true,
    state,
    actions,
    mutations,
    gettters: {}
}